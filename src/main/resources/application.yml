server:
  port: 8080
spring:
  application:
    name: reggie_take_out
  mail:
    host: smtp.qq.com
    username: 1809955731@qq.com
    password: zfmvseutuidjgagh
    default-encoding: utf-8
    properties:
      mail:
        smtp:
          ssl:
            enable: true
  shardingsphere:
    datasource:
      names:
        master,slave
      # 主数据源
      master:
        type: com.alibaba.druid.pool.DruidDataSource
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://192.168.159.100:3306/reggie?characterEncoding=utf-8&serverTimezone=Asia/Shanghai
        username: root
        password: 111111
      #从数据源 ，从库可以有多个
      slave:
        type: com.alibaba.druid.pool.DruidDataSource
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://192.168.159.101:3306/reggie?characterEncoding=utf-8&serverTimezone=Asia/Shanghai
        username: root
        password: 111111
    masterslave:
      # 读写分离配置
      # 负载均衡配置 ，轮询
      load-balance-algorithm-type: round_robin
      # 最终的数据源名称
      name: dataSource
      # 主库数据源名称
      master-data-source-name: master
      # 从库数据源名称列表，多个逗号分隔
      slave-data-source-names: slave
    props:
      sql:
        show: true #开启SQL显示，默认false
  main:
    allow-bean-definition-overriding: true   #允许bean覆盖
  redis:
    host: localhost
    port: 6379
    #password:
    database: 0
  cache:
    redis:
      time-to-live: 1800000  #设置缓存有效时间
#  aop:
#    proxy-target-class: true
mybatis-plus:
  configuration:
    #在映射实体或者属性时，将数据库中表名和字段名中的下划线去掉，按照驼峰命名法映射 user_name->userName,ASSIGN_ID自动生成id
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: ASSIGN_ID
reggie:
  path: c:\image\


